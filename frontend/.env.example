# API base URL for backend requests.
# 可以填写 http://localhost:8080 或其他实际部署地址。
VITE_API_BASE_URL=http://localhost:8080

# 是否启用模拟数据（true/false）。
# 启用后所有 API 请求会被内置的模拟适配器拦截并返回测试数据，
# 适合在后端接口未就绪时预览和测试前端页面。
VITE_ENABLE_MOCK=false
